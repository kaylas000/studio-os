# ВЫЛЕТ · спецтехника на смену

Клиентский проект на STUDIO OS: автовышки, автокраны, манипуляторы и земтехника
с заказом работ. Архетип — **Cyber-Tech** (HUD-панели, срезанные углы, моноширинная
телеметрия), анти-слоп-контур включён в сборку.

```bash
npm run dev --workspace=projects/client-vylet     # порт 3000
node ../../core-engine/bin/studio.js audit .      # аудит 9 стандартов (или npm run audit)
npm run seo                                       # перегенерить title/description/JSON-LD в index.html
npm run build && node scripts/smoke.mjs           # сборка + смоук-проверка монтирования
```

## Что менять под клиента (порядок, а не список)

1. **`src/content/catalog.ts`** — модели, год, наработка, цены за смену, количество свободных единиц.
   Все числа в интерфейсе приходят отсюда: карточки, таблица, калькулятор, `priceRange` в Schema.org.
2. **`src/content/copy.ts`** — тексты. Формула PAS и факт-плотность проверяются аудитом:
   оценочные слова («качественно», «индивидуальный подход», «широкий спектр») роняют Originality Score.
3. **Часы, а не только смены.** У единицы может быть `minHours` (у компактной АГП-16 — 2 часа):
   калькулятор считает время по прайсу часа, после `SHIFTS.hours` включает цену смены, пресеты —
   2 ч · смена · сутки · двое суток. Значение `minHours` становится нижней границей слайдера, отсюда же
   строка «Минимальный заказ» в карточке единицы.
4. **`src/content/seo.config.ts`** — title 30–70, description 70–165 символа, адрес, телефон, `sameAs`, FAQ.
   После правки — `npm run seo`, иначе мета в `index.html` разъедется с контрактом.
5. **`src/content/brand.ts`** — палитра поверх архетипа. Это единственный файл, где меняются цвета:
   он же красит CSS-переменные, и он же проходит APCA-гейт (`studio audit` не даст поставить нечитаемый цвет).

## Фото: приёмка по одному снимку

Прислали фото — кладите в любую папку и запускайте:

```bash
node ../../core-engine/bin/studio.js photo ~/IMG-20260902-WA0016.jpg \
  --slot=aerial-16 --link=agp-16 --frame=3/4 --focus=auto --og
```

`--slot` и `--link` берутся из каталога: `node ../../core-engine/bin/studio.js photo --list`
покажет слоты, кадры и подсказку для следующего снимка. Компактной АГП-16 нужен вертикальный
кадр 3/4 — телескопик в кадре целиком, иначе превью в сетке парка обрежет стрелу.

Что делает команда (`core-engine/lib/photos.js`, sharp + `--dry` для проверки без записи):

| Шаг | Зачем |
|---|---|
| поворот по EXIF-ориентации | телефонный снимок иначе ляжет на бок |
| срез чёрных полос (letterbox) | скриншоты и WhatsApp-кадры с полями; тёмную ночную смену не трогает |
| крой под кадр слота (`--frame=3/2`, `4/3`, `3/4`, `16/10`, `--native`) | рамка `.photo` обязана совпадать с пропорцией: иначе CLS-прыжок |
| `--focus=top\|center\|bottom\|auto` | `auto` ищет полосу с максимумом деталей — стрела не отрежется |
| снятие EXIF/GPS | в метаданных телефона — точка выезда на объект |
| вес ≤ 220 КБ (`--max=1600 --quality=82`) | бюджет одного снимка в первом экране |
| `--link=<id или #Заголовок>` | вписывает `photo` и `photoRatio` в `catalog.ts` / `copy.ts` — код не трогаем |
| `--og` | та же фотография → `public/og.jpg` 1200×630 для соцсетей (на него ссылается `seo.config.ts`) |
| `--blur=x,y,w,h` (доли кадра) | госномер и лица посторонних: `--blur=0.60,0.72,0.20,0.05` |

Список свободных слотов: `studio photo --list`. Проверка без записи: `--dry`.
После трёх-четырёх снимков — `npm run seo && npm run build && node scripts/smoke.mjs`.

Файлы живут в `src/assets/photos/<slot>.jpg` (имя = значение `photo` в каталоге), их `PhotoSlot`
подхватывает через `import.meta.glob` — правка кода не нужна. Пока файла нет, на месте слота
техническая плашка с именем и требуемым кадром: макет не выглядит сломанным.

## Что уже встроено из библиотеки

| SYS | Реализация в проекте |
|---|---|
| 01 | `engine/useMotion.ts` — Lenis + GSAP + ScrollTrigger, `MotionGuard` (reduced-motion, Data Saver, батарея, слабый GPU) |
| 02 | тексты и палитра под `ClicheDetector` + `GradientSlopDetector`, Originality Score 92/100 |
| 03 | `fluid-system.css`, `safe-area.css`, таблица в `ViewportMatrix` (19 вьюпортов), touch-target ≥ 44 px |
| 04 | все отступы — `var(--space-*)`; `Box/Stack/Section` из `primitives.tsx` |
| 05 | `components/Intro.tsx` на `IntroEngine`: Skip + Esc, лимит по FPS, полный `dispose`, статика без WebGL |
| 06 | `content/seo.config.ts` → валидация контракта + JSON-LD (LocalBusiness, Service, BreadcrumbList, FAQPage), h1 в первом ответе |
| 07 | архетип cyber-tech + `brand.ts`, контраст считается по APCA (мин. 75 Lc для body) |
| 08 | PAS в текстах, факт-плотность и читаемость — метрики в `studio.audit.json` |
| 09 | `StaticCodeAuditor`: rAF-отмена, `dispose()` WebGL, Error Boundary, alt/размеры у `<img>` |

## Черновые данные

Модели, цены, телефон, ИНН и адреса в каркасе — **заглушки** (см. `FOOTER.dataNotice`).
Перед публикацией заменить на реестровые и поставить `canonical` на рабочий домен.
Форма заявки собирает письмо на стороне браузера: бэкенд подключается одним `fetch`
в `10-OrderForm.tsx`, контракт payload уже зафиксирован.

## Журнал входящих от заказчика

Факты принимаются без уточняющих вопросов: пришло — внесено в источник правды,
остальное каркас пересоберёт сам (`npm run seo` после правки `seo.config.ts`).

| Дата | Что пришло | Куда вошло | Статус |
|---|---|---|---|
| 02.09 | «Где не достанет лестница — достанет автовышка, компактная, одна из самых маленьких» | `catalog.ts` → `agp-16`, кикер и тикер | принято |
| 02.09 | Покраска коттеджей и домов, мойка окон, сруб, деревья — «любая работа от слова совсем» | `SERVICES` (facade, clearing) | принято |
| 02.09 | «Работаю со своим либо с вашим мастером», «24/7» | `CALC_COPY.limits`, `FOOTER.hours`, `ORDER.lede`, FAQ-note, JSON-LD `openingHours` | принято |
| 02.09 | Вызов на 2 часа, смену, сутки | `Unit.minHours`, пресеты калькулятора, `FLEET_COPY.footnote`, FAQ | принято |
| 02.09 | Габарит по высоте проезда 2,8 м, компрессор стравить колёса | `agp-16.ground` | принято |
| 02.09 | Вверх 16 м, вбок 16,5 м — пятый этаж советского дома | `agp-16.outreach` | принято |
| 02.09 | `IMG-20260902-WA0018.jpg` | фотоприёмка | **файл не долетел до песочницы** |
| 02.09 | `IMG-20260902-WA0016.jpg` — коттедж, стрела слева снизу | фотоприёмка | **файл не долетел** |
| 03.09 | `IMG-20260902-WA0019.jpg` — ISUZU ELF у звонницы, вывешен на 4 лапы по брусчатке | `agp-16.outreach`/`ground`, SERVICES(facade): купола, кресты, колокола | содержимое учтено, **файл не долетел** |
| 03.09 | Номер госномера не размывать | `photos.js --blur` не применяется к этому слоту | принято как постоянное правило |
| 03.09 | Автовышки от 14 до 70 м | `agp-14` (вместо 12 м), `agp-70`; `AERIAL_RANGE` | принято |
| 03.09 | Краны 25–50 т | `kv-50` вместо 100-тонного, `CRANE_RANGE` | принято |
| 03.09 | Манипуляторы разной длины стрелы и грузоподъёмности | `MANIP_RANGE` + `MANIP_BOOM` из парка | принято как «3,2–7 т, стрела 9–14 м» — ждёт расширения парка |
| 03.09 | — | жёлоб 125/150 мм и труба 90 мм в услуге водостока — стандартные типоразмеры, не со слов заказчика: подтверди, что носишь |
| 03.09 | «от мини экскаватора…» — сообщение оборвалось | `mini-2` (2,8 т), `EARTH_RANGE` | принято частично, хвост жду следующим сообщением |
| 03.09 | «по работе можем работать с кондиционерами, водосточной, электрикой, наружний СИП», «наружная реклама», «пилка деревьев» (пересыл от «Серега Вышка») | SERVICES: +3 карточки — `signage`, `hvac`, `drainage`; `clearing` переименована на «пилка и кронирование»; 3 оффера в JSON-LD | принято |
| 03.09 | `IMG-20260902-WA0020/17/21.jpg` — три кадра с объектов | приёмка фото | **файлы не долетели** (пятый снимок подряд), разбор кадров выше |
| 03.09 | 4 снимка одним сообщением (WA0012 дважды, WA0005, WA0014): ночь на площади с баннерами, ночной дом с гирляндой по карнизу, дневной коттедж в зазоре между пристройками | `SERVICES.signage` += «подсветка и гирлянды по карнизу», `agp-16.outreach` += «карниз коттеджа в зазоре между пристройками» | содержимое учтено, **байты не долетели** |
| 03.09 | — | `studio photo` принимает пачку: `--auto` по свободным слотам, дубли по имени, `--link` из каталога | сделано |
| 03.09 | — | факт-плотность после +3 услуг упала до 27 слов на цифру: карточки сжаты, убраны дубли (простой в 2 местах, «40 минут»), FAQ и подвал укорочены → 24 | принято |

Фото, которые долетят, обрабатываются без вопросов одной командой — файл кладётся
в `/home/user/uploads/` (или в `/home/user/`), дальше:

```bash
node core-engine/bin/studio.js photo <файл> --list            # свободные слоты
node core-engine/bin/studio.js photo <файл> --slot=aerial-16 --link=agp-16 \
     --frame=3/4 --focus=auto --og
```

Пока файла нет, слот остаётся placeholder-ом: рамка, подпись и пропорция уже
зарезервированы, вёрстка от отсутствия кадра не прыгает.

### Разбор присланных кадров (что куда пойдёт)

| Файл | Что на снимке | Слот |
|---|---|---|
| `IMG-20260902-WA0014.jpg` | день, коттедж: люлька у карниза второго этажа в зазоре между пристройкой и гаражом, рядом леса с сеткой | `aerial-16` — основной кадр машины в работе |
| `IMG-20260902-WA0012.jpg` (прислан дважды) | ночь, площадь под прожекторами: ISUZU целиком, стрела сложена над кабиной, баннерные линии на фоне | `--og` (обложка ссылки) — дубль по имени приёмка отсекает сама |
| `IMG-20260902-WA0005.jpg` | ночь, дом: люлька у карниза, по которому развешана гирлянда, подсветка уже горит | `case-facade` — настоящий кейс «подсветка и гирлянды по карнизу» |
| `IMG-20260902-WA0020.jpg` | люлька в кронах берёзы, оператор с пилой, вывешенная лапа на плитке | `aerial-16` (альтернатива WA0014, если нужен спил) |
| `IMG-20260902-WA0017.jpg` | фасад с вывесками «ФИТНЕС2 / ELUX / АПТЕКА», второй этаж | `case-signage` — слот заведу после того, как подтвердишь, что это ваш объект: чужую вывеску под своим кейсом не публикуем |

Шильд на стреле читается частично (что-то вроде «ТАГАНИ…»), на другой машине — «АКРО…».
Марку в `maker` не вписываю: угадать бренд — это не черновая цифра, это враки.

Ни один из четырёх файлов до песочницы не дошёл (`/home/user/uploads/` не создан, `find`
по диску пуст, сети наружу нет). Снимки я вижу в переписке, байтов — нет, поэтому приёмку
не выполнял, а из кадров взял только то, что на них видно: «подсветка и гирлянды по
карнизу» в услуге рекламы и «карниз коттеджа в зазоре между пристройками» в описании
АГП-16.

Пачка принимается одной командой: `--auto` берёт свободные слоты по порядку, сам
подставляет `--link` из каталога и режет дубликаты по имени — WhatsApp присылает одно и то
же по два раза:

```bash
node core-engine/bin/studio.js photo /home/user/uploads --auto --dry   # разбор, ничего не пишет
node core-engine/bin/studio.js photo /home/user/uploads --auto         # приёмка всей пачки
node core-engine/bin/studio.js photo /home/user/uploads --slots=aerial-16,case-facade --og
```

Все три пришли вертикальными с чёрными полями WhatsApp: `trimProfile()` сначала
срежет поля, потом `--frame` подгонит под кадр слота. Номер на бампере не размываем
(постоянное правило). Лицо оператора в люльке на WA0020 читается — это кадр для
карточки парка, при публикации в соцсетях скажи, и прогоню тот же файл через
`--blur` отдельной копией.
